# Zero-Day FYP — Python dependencies
#
# A venv is NEVER portable: it hardcodes absolute paths to the machine that
# created it. Do not commit venv/ and do not copy it between machines.
# Each machine builds its own venv from this file. See README.md.
#
# Versions below are pinned to what the working laptop venv actually had,
# so PC and laptop resolve to identical builds.

# ── Core numerics / data ─────────────────────────────────────────────
numpy==2.4.4
pandas==3.0.3
scipy==1.18.0
scikit-learn==1.9.0

# ── Plotting ─────────────────────────────────────────────────────────
matplotlib==3.11.0

# ── Deep learning ────────────────────────────────────────────────────
# CUDA 12.8 build (tested on RTX 3090, driver 591.86 / CUDA 13.1).
# Requires the PyTorch index below.
# On a machine with NO NVIDIA GPU, delete the --extra-index-url line and use
# the plain CPU build instead:   torch==2.11.0
# NOTE: results are device-sensitive (see CLAUDE.md gotcha #24) — record which
# build produced every published number.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128

# ── Week 5: host HMM baseline (detection/exp_host_ablation.py) ──────
hmmlearn==0.3.3

# ── Explainability (Person C seam / used by detection/shap_explainer.py) ──
# Pinned 2026-09-20 (audit): unpinned shap resolved differently per machine.
shap==0.52.0

# ── Week 3: graph construction + GNN (detection/graph_builder.py) ────
# PyTorch Geometric is the toolkit locked in the reference PDF §10.
# Pinned 2026-09-20 (audit).
torch-geometric==2.8.0.post1
